/**
 * S3 / MinIO client (AWS SDK v3).
 *
 * Placeholder for M1 — picture upload to object storage is implemented in a
 * later milestone. Construct and export the S3Client here, configured from
 * `config/env.ts`.
 */
export {};
