/**
 * GeoJSON helpers — zod schemas for `Point` / `Polygon` (WGS84 / EPSG:4326)
 * plus (de)serialization helpers for Prisma `Json` geography columns.
 *
 * Placeholder for M1 — fully implemented in the DB-access milestone.
 */
export {};
