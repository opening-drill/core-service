/**
 * Picture storage — uploads event PNGs to S3/MinIO and returns the
 * `object_key` / `bucket` used to persist a `picture` row.
 *
 * Placeholder for M1 — implemented in the storage milestone.
 */
export {};
