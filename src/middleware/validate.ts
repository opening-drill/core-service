/**
 * Zod request-validation middleware.
 *
 * Placeholder for M1 — implemented alongside the entity modules: validate
 * `body`/`query`/`params` against a zod schema and surface failures to the
 * central error handler as `ZodError` → HTTP 400.
 */
export {};
