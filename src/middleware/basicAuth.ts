/**
 * HTTP Basic identity middleware.
 *
 * Placeholder for M1 — implemented in the Auth milestone: parse the
 * `Authorization: Basic` header, resolve the `user`, attach it to the request.
 */
export {};
