/**
 * RBAC permission guard.
 *
 * Placeholder for M1 — implemented in the Auth milestone: `authorize(permission)`
 * resolves user → roles → permissions and enforces the `permission` enum per route.
 */
export {};
