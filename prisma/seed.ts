/**
 * Database seed — permissions, baseline roles, and an admin user.
 *
 * Placeholder for M1 — the Prisma schema lands in M2, after which this seeds
 * the RBAC baseline. Kept runnable so `npm run prisma:seed` exists today.
 */
function main(): void {
  process.stdout.write('Seed not yet implemented — pending the M2 Prisma schema.\n');
}

main();
